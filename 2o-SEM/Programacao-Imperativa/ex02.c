#include <stdio.h>

int main() {
    double PI = 3.14;
    double radius = 10.0;
    double area = PI * (radius * radius);

    printf("%f", area); // "%f" means that what will be printed is float or double 
}